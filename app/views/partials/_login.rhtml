 <% if logged_in? %>
        Logged as <b><%= current_user.login %></b>
        
        &nbsp;|&nbsp; <%= link_to "logout", logout_url %>
        <br/>
   <%else%>
       <%= link_to "login", login_url %>
  <%end%>     